# Server Pod environment.
RUNPOD_MODE=server
DB_PATH=/workspace/server_state/runpod_distributed.sqlite
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
WORKER_TOKEN=replace-with-long-random-token
RESET_DB=0

# Optional, used by add-worker / terminate-drained-pods from the server CLI.
RUNPOD_API_KEY=
